{
    "name": "muse-app",
    "version": "0.1.0",
    "private": true,
    "dependencies": {
        "@types/jest": "^29.5.4",
        "@types/node": "^18.17.14",
        "typescript": "^5.2.2",
        "esbuild": "^0.19.2",
        "@aws-sdk/lib-dynamodb": "^3.405.0",
        "@types/aws-lambda": "^8.10.119"
    },
    "scripts": {
        "lint": "npx eslint app --ext .ts --max-warnings 0",
        "prettier": "npx prettier --write app"
    },
    "workspaces": [
        "./client",
        "./server"
    ]
}
